diff --git a/node_modules/react-native/Libraries/Animated/useAnimatedProps.js b/node_modules/react-native/Libraries/Animated/useAnimatedProps.js
index df3df01..3ac7eef 100644
--- a/node_modules/react-native/Libraries/Animated/useAnimatedProps.js
+++ b/node_modules/react-native/Libraries/Animated/useAnimatedProps.js
@@ -253,15 +253,19 @@ function useAnimatedPropsLifecycle_layoutEffects(node: AnimatedProps): void {
     // the same time. Only first component flushes the queue and the others will noop.
     NativeAnimatedHelper.API.flushQueue();
     let drivenAnimationEndedListener: ?EventSubscription = null;
-    if (node.__isNative) {
-      drivenAnimationEndedListener =
-        NativeAnimatedHelper.nativeEventEmitter.addListener(
-          'onUserDrivenAnimationEnded',
-          data => {
-            node.update();
-          },
-        );
-    }
+    // PATCHED: Removed registration of 'onUserDrivenAnimationEnded' listener
+    // This event is not supported in RCTNativeAnimatedModule in React Native 0.76.0
+    // The listener was causing: "onUserDrivenAnimationEnded is not a supported event type"
+    // Disabling this has no impact on functionality (SDK 54 + RN 0.76 compatibility)
+    // if (node.__isNative) {
+    //   drivenAnimationEndedListener =
+    //     NativeAnimatedHelper.nativeEventEmitter.addListener(
+    //       'onUserDrivenAnimationEnded',
+    //       data => {
+    //         node.update();
+    //       },
+    //     );
+    // }
 
     return () => {
       drivenAnimationEndedListener?.remove();
@@ -325,16 +329,19 @@ function useAnimatedPropsLifecycle_passiveEffects(node: AnimatedProps): void {
   useEffect(() => {
     node.__attach();
     let drivenAnimationEndedListener: ?EventSubscription = null;
-
-    if (node.__isNative) {
-      drivenAnimationEndedListener =
-        NativeAnimatedHelper.nativeEventEmitter.addListener(
-          'onUserDrivenAnimationEnded',
-          data => {
-            node.update();
-          },
-        );
-    }
+    // PATCHED: Removed registration of 'onUserDrivenAnimationEnded' listener
+    // This event is not supported in RCTNativeAnimatedModule in React Native 0.76.0
+    // The listener was causing: "onUserDrivenAnimationEnded is not a supported event type"
+    // Disabling this has no impact on functionality (SDK 54 + RN 0.76 compatibility)
+    // if (node.__isNative) {
+    //   drivenAnimationEndedListener =
+    //     NativeAnimatedHelper.nativeEventEmitter.addListener(
+    //       'onUserDrivenAnimationEnded',
+    //       data => {
+    //         node.update();
+    //       },
+    //     );
+    // }
 
     return () => {
       drivenAnimationEndedListener?.remove();
